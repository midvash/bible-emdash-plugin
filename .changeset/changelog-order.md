---
"@midvash/emdash-plugin-bible": patch
---

Docs: fix `CHANGELOG.md` ordering — the prose intro had drifted between version
entries (Changesets prepends new versions right after the H1). Removed it so
entries stay newest-first.
